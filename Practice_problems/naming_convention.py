import power_of_2  # files and modules should be in lowercase and underscore is used for white space


def naming_func():  # functions should be in lower case and underscore is used for white space
    variable_one = 24  # local and public variables should be in lower case and underscore is used for white space
    PI_VALUE = 3.14  # constants are named in upper case and underscore is used for white space
    return variable_one * PI_VALUE


if __name__ == "__main__":
    a = naming_func()
    print(a)
